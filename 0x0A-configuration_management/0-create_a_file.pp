# this is a puppet script that makes a file school in the tmp directory and
# adds some content to the made file

file { 'school':
  path    => '/tmp/school',
  mode    => '0744',
  owner   => 'www-data',
  group   => 'www-data',
  content => 'I love Puppet',
}
